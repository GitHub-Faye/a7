# 任务19.1 - 扩展Courseware模型以存储文件元数据的实现计划

## 任务概述

需要在当前的课程管理系统中扩展Courseware模型，以便能够存储和管理文件元数据，同时移除所有版本控制相关的字段和逻辑。

## 当前状态分析

1. **现有Courseware模型**：
   - 目前Courseware模型已经存在，包含基本字段如title、content、type等
   - 当前模型没有专门的文件元数据存储功能
   - 文件管理功能尚未实现

2. **项目媒体文件配置**：
   - 项目中已有STATIC_URL和STATIC_ROOT的配置，但似乎缺少MEDIA_URL和MEDIA_ROOT的配置
   - 测试代码中使用了MEDIA_ROOT和MEDIA_URL，但这些可能需要在正式设置中添加

## 实现计划

### 1. 创建CoursewareFile模型

创建一个新的模型来专门存储文件元数据，与Courseware模型建立关系：

```python
class CoursewareFile(models.Model):
    """
    课件文件模型，存储与课件相关的文件元数据
    """
    courseware = models.ForeignKey(
        Courseware, 
        on_delete=models.CASCADE, 
        related_name='files',
        verbose_name='所属课件'
    )
    file = models.FileField(
        upload_to='coursewares/%Y/%m/',
        verbose_name='文件'
    )
    file_name = models.CharField(max_length=255, verbose_name='文件名')
    file_size = models.PositiveIntegerField(verbose_name='文件大小(字节)')
    file_type = models.CharField(max_length=100, verbose_name='文件类型')
    upload_time = models.DateTimeField(auto_now_add=True, verbose_name='上传时间')
    
    class Meta:
        verbose_name = '课件文件'
        verbose_name_plural = '课件文件'
        ordering = ['-upload_time']
        indexes = [
            models.Index(fields=['courseware', 'file_type'], name='cwfile_cw_type_idx'),
            models.Index(fields=['upload_time'], name='cwfile_upload_time_idx')
        ]
    
    def __str__(self):
        return self.file_name
        
    def save(self, *args, **kwargs):
        # 如果是新创建的记录，自动设置file_name, file_size和file_type
        if not self.pk:
            self.file_name = self.file.name.split('/')[-1]
            self.file_size = self.file.size
            self.file_type = self.file.content_type if hasattr(self.file, 'content_type') else ''
        super().save(*args, **kwargs)
```

### 2. 扩展Courseware模型

更新现有的Courseware模型，添加标识是否包含文件的字段，并确保移除所有版本控制相关字段和逻辑：

```python
class Courseware(models.Model):
    # ...现有字段保持不变...
    
    # 新增字段，标识是否有关联文件
    has_files = models.BooleanField(default=False, verbose_name='包含文件')
    
    # ...其他方法和Meta类保持不变...
    
    def update_has_files(self):
        """更新has_files字段状态"""
        has_files = self.files.exists()
        if self.has_files != has_files:
            self.has_files = has_files
            self.save(update_fields=['has_files'])
```

### 3. 添加媒体文件配置

在settings.py中添加必要的媒体文件配置：

```python
# 媒体文件配置
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
```

在主urls.py中添加媒体文件的URL配置：

```python
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    # ... 现有URL配置 ...
]

if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
```

### 4. 创建数据库迁移文件

生成并应用数据库迁移：

```bash
python manage.py makemigrations courses
python manage.py migrate courses
```

### 5. 更新Admin配置

更新课程应用的admin.py文件，添加CoursewareFile的管理功能：

```python
@admin.register(CoursewareFile)
class CoursewareFileAdmin(admin.ModelAdmin):
    list_display = ('file_name', 'courseware', 'file_size', 'file_type', 'upload_time')
    list_filter = ('file_type', 'upload_time')
    search_fields = ('file_name', 'courseware__title')
    readonly_fields = ('file_size', 'file_type', 'upload_time')
```

### 6. 编写单元测试

创建测试文件或更新现有测试，验证文件关联功能：

```python
def test_courseware_file_creation(self):
    """测试创建课件文件并验证关联"""
    # 创建测试文件
    file_content = b"test file content"
    test_file = SimpleUploadedFile("test_file.txt", file_content, content_type="text/plain")
    
    # 创建文件记录
    courseware_file = CoursewareFile.objects.create(
        courseware=self.courseware,
        file=test_file
    )
    
    # 验证文件元数据是否正确保存
    self.assertEqual(courseware_file.file_name, "test_file.txt")
    self.assertEqual(courseware_file.file_size, len(file_content))
    self.assertEqual(courseware_file.file_type, "text/plain")
    
    # 验证has_files状态更新
    self.courseware.update_has_files()
    self.assertTrue(self.courseware.has_files)
```

## 注意事项

1. **媒体存储配置**：
   - 确保MEDIA_URL和MEDIA_ROOT在settings.py中正确配置
   - 在生产环境中，考虑配置安全的文件访问机制

2. **S3集成准备**：
   - 系统设计应该考虑未来与AWS S3或其他云存储集成的可能性
   - 可以添加注释说明后续如何改用云存储

3. **URL路由设置**：
   - 确保添加适当的URL路由以支持文件上传和下载功能
   - 文件访问URL应当遵循RESTful设计风格

4. **文件类型验证**：
   - 实现文件类型验证机制，限制只接受PDF、Word、PPT等指定类型的文件
   - 可以在模型或表单级别添加验证逻辑

5. **文件组织**：
   - 使用合理的文件命名和存储结构（如使用UUID作为文件名前缀）
   - 按年月组织文件目录结构，便于后期管理

这个计划提供了实现任务19.1所需的全部步骤，包括模型设计、配置更新、迁移文件创建和单元测试编写，以确保功能的完整性和正确性。
